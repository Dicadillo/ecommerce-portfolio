[pytest]
testpaths = tests
addopts = -ra --strict-config --strict-markers
markers =
    smoke: comprobaciones críticas y rápidas del servicio
    regression: cobertura funcional y casos negativos detallados
log_level = INFO
log_format = %(asctime)s | %(levelname)s | %(name)s | %(message)s
